# 现场工作 PWA

入口 `/field/`，员工使用现有账号登录。管理系统菜单和工单详情提供入口；添加到主屏幕后可单独打开。页面底部为拍照、工单、台账、我的。

## 拍照和归档

拍照前刷新定位，固定工单、员工、时间、时区、定位精度和备注。实时相机记录设备拍摄时间；系统相机/文件选择明确标记为选图记录，不宣称照片原始时间已核验。时间和定位来自设备，水印不构成防伪认证。

压缩后的水印照片首先写入 IndexedDB；事务成功才显示已保存。在线尝试上传；服务端确认后删除手机待传副本。断网、登录过期、权限失效或服务器错误时保留副本，可下载备份。账号切换不会把原账号照片传到新账号。请勿清理浏览器存储；存储配额不足会明确报错。

服务端再次压缩（最长边 1800px、JPEG quality 78），生成缩略图，保存到：

`工单编号/pictures/拍摄日期/YYYYMMDD_HHMMSS-u员工ID-照片UUID.jpg`

归档日期按照片保存的 IANA 时区计算，默认手机时区，可在“我的”更改。补传不改变照片日期。上传日期另行记录。照片 UUID + 员工 ID 去重，收到请求但回执丢失时可安全重试。现有照片整理程序识别该命名格式，不会重命名台账文件。

## 定位和离线边界

仅在前台定位；打开、回到前台、拍摄前检查。连续两次精度较好的前台定位发现超出范围时弹窗提醒，员工确认原因或选择其他工单。默认阈值 500 米，可用服务器环境变量 `FIELD_DISTANCE_METERS` 设置（100–10000 米）。定位误差会从距离判断中扣除。精度超过 100 米或工单缺少坐标时，拍照前要求人工确认。没有定位则提示重试，不伪造坐标。

后台或锁屏后相机停止，上传可能暂停。重新打开并联网后继续补传。Service Worker 仅缓存不含员工数据的页面外壳和静态资源，不缓存 API、照片、登录页或审批页。新设备需要先联网登录并打开一次，才能离线重开。

无对应工单时，有创建权限者进入现有新建工单页面；其他员工可提交带现场坐标的建单申请给管理员、经理。新建/申请需要联网。

## 照片台账和权限

PWA 台账和后台 `/reports/field-photos` 可按关键词、日期、工单查询并导出 Excel。单次最多展示 2000 张，超限导出要求缩小范围。照片上传沿用日报创建权限，读取沿用工单权限；普通员工/外部员工只看到自己的照片，管理角色按原工单访问范围查询。

## 员工证书查询

后台报表 `/reports/user-certificates` 从现有员工附件生成清单，一份附件一行，支持员工、姓名/文件名、账号状态及有无附件筛选。使用既有附件访问规则，可预览、下载，并使用通用 Excel 导出。现有附件尚无证书类型、签发日期或有效期字段，清单不推断文件内容或证书有效性。

## 验证

`python -m unittest discover -v` 覆盖权限、重复上传、元数据校验、压缩、日期归档、照片整理兼容和证书查询。发布前还使用隔离测试库和浏览器模拟相机/GPS验证离线重开、账号隔离及上传回执丢失。模拟测试不替代 iPhone/Android 真机相机与定位试用。
