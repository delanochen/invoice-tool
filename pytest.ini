[pytest]
; exclude local backup/debug dirs to avoid duplicate-basename test collection errors
norecursedirs = _* .codesight .git.broken* .playwright-cli .workbuddy .vscode .release data .venv* venv build dist
